@model Music.Models.Album

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Album</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Band.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Band.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ReleaseDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ReleaseDate)
        </dd>
    </dl>
</div>

<table>
    @foreach (var track in ViewBag.trackList)
    {
        <tr>
            <td>
                @track.Title;
                @Html.DisplayNameFor(model => model.Track.Title)
            </td>

            <td>
                @Html.DisplayFor(model => model.Track.Title)
            </td>
        </tr>
    }
</table>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @*@Html.ActionLink("Add Tracks", "Create", "Track", new { id = Model.Id }, null) |*@
    @Html.ActionLink("Back to List", "Index")
</p>
